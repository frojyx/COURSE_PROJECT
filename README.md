# NewMusicCatalog

Музыкальный каталог — настольное приложение для управления коллекцией музыкальных треков с поддержкой импорта из Yandex Music.

## Описание

Приложение позволяет управлять коллекцией музыкальных треков: добавлять, редактировать, удалять и искать треки по различным критериям. Поддерживается интеграция с Yandex Music для импорта треков, работа с MP3 файлами и их метаданными, а также экспорт и импорт каталога в текстовые файлы.

## Возможности

- 🎵 **Управление треками**: добавление, редактирование, удаление треков
- 🔍 **Расширенный поиск**: поиск по названию, исполнителю, альбому, жанру, году и длительности
- 🎨 **Визуальная подсветка**: специальная подсветка треков из Yandex Music
- 🌐 **Интеграция с Yandex Music**: поиск и импорт треков из Yandex Music API
- 💾 **Сохранение данных**: экспорт/импорт каталога в текстовые файлы (TXT и CSV)
- 🔄 **Автосохранение**: автоматическое сохранение каталога после изменений
- 📊 **Сортировка**: сортировка треков по различным полям (название, исполнитель, год, длительность)

## Технологии

- **C++23**: основной язык программирования
- **Qt6**: графический интерфейс (Widgets) и сетевая функциональность (Network)
- **CMake**: система сборки

## Качество кода

Качество кода проекта контролируется с помощью [SonarCloud](https://sonarcloud.io/summary/overall?id=frojyx_COURSE_PROJECT&branch=master).

## Структура проекта

```
NewMusicCatalog/
├── includes/          # Заголовочные файлы (.h)
├── src/              # Исходные файлы (.cpp)
├── mainwindow.ui     # UI-файл Qt Designer
├── CMakeLists.txt    # Файл конфигурации CMake
└── README.md         # Документация
```

### Основные компоненты

- **MusicCatalog**: основной класс каталога, управляющий коллекцией треков
- **Track**: класс, представляющий музыкальный трек
- **TrackRepository**: репозиторий для хранения и управления треками
- **TrackSearcher**: класс для поиска треков по различным критериям
- **TrackSorter**: класс для сортировки треков
- **YandexMusicAPI**: клиент для работы с Yandex Music API
- **YandexMusicIntegrator**: интегратор для импорта треков из Yandex Music
- **FileManager**: управление сохранением и загрузкой каталога
- **TrackTableHighlighter**: подсветка строк в таблице треков

## Сборка

### Требования

- Qt6 (модули Widgets и Network)
- CMake 3.16 или выше
- Компилятор с поддержкой C++23 (MSVC, GCC, Clang)



### Сборка в Qt Creator

1. Откройте проект в Qt Creator (выберите файл `CMakeLists.txt`)
2. Настройте комплект (Kit) с Qt6
3. Нажмите кнопку "Собрать" (Build) или используйте Ctrl+B
4. Запустите приложение (Ctrl+R)

## Использование

### Основной экран

Главный экран содержит таблицу всех треков с возможностью:
- Просмотра информации о треках (название, исполнитель, альбом, год, жанр, длительность)
- Редактирования треков (кнопка "Редактировать")
- Удаления треков (кнопка "Удалить")
- Открытия файлов треков (кнопка "Открыть")
- Сортировки по столбцам (клик по заголовку столбца)

### Добавление трека

1. Нажмите кнопку "Добавить трек"
2. Заполните форму с информацией о треке
3. Опционально выберите MP3 файл для автоматического заполнения метаданных
4. Нажмите "Добавить"

### Поиск треков

1. Используйте форму поиска на главном экране
2. Заполните нужные поля (название, исполнитель, альбом, жанр, год, длительность)
3. Нажмите "Поиск"
4. Найденные треки будут подсвечены в таблице

### Импорт из Yandex Music

1. Перейдите в раздел "Yandex Music"
2. Введите поисковый запрос
3. Выберите треки из результатов поиска
4. Нажмите "Импортировать"
5. Импортированные треки будут добавлены в каталог с автоматическим сохранением

## Формат файлов

### Формат TXT каталога

Каталог сохраняется в текстовом формате с разметкой:

## Обработка ошибок

Приложение использует иерархию исключений для обработки ошибок:

- **MusicCatalogException**: базовое исключение каталога
- **TrackException**: ошибки работы с треками
- **FileException**: ошибки работы с файлами
- **ValidationException**: ошибки валидации данных
- **ParseException**: ошибки парсинга данных

Все ошибки обрабатываются с информативными сообщениями для пользователя.

## Архитектура

Проект построен на принципах объектно-ориентированного программирования с четким разделением ответственности:

- **Модель данных**: `Track`, `TrackRepository` — хранение и управление данными
- **Бизнес-логика**: `MusicCatalog`, `TrackSearcher`, `TrackSorter` — операции с каталогом
- **Интеграция**: `YandexMusicAPI`, `YandexMusicIntegrator` — работа с внешними сервисами
- **Файловые операции**: `FileManager`, `MP3MetadataReader`, `MP3FileNameParser` — работа с файлами
- **Пользовательский интерфейс**: `MainWindow` — взаимодействие с пользователем



